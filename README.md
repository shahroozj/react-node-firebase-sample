# React + Firebase Notes App

A beautiful full-stack notes application built with:

- **React + Vite + TypeScript**
- **Firebase Authentication & Cloud Firestore**
- **Firebase Functions (Node.js Express API)**
- **Tailwind CSS** for modern UI styling

Users can securely **sign up / log in**, then **create, view, and delete notes**, each tied to their own Firebase account.

---